<% data = parse_ai_analysis(check.ai_response) %>

<!-- Structured Analysis Content -->
<div class="space-y-3">
  <!-- REFUZATE Section -->
  <% if data[:refuzate].any? %>
    <div>
      <div class="text-xs font-medium text-error mb-2 flex items-center gap-1">
        <span class="text-sm">❌</span>
        <span>Criterii blocante (<%= data[:refuzate].size %>)</span>
      </div>
      <ul class="list-disc list-inside space-y-1.5 text-xs leading-relaxed ml-1">
        <% data[:refuzate].each do |item| %>
          <li>
            <span class="font-medium"><%= item[:criterion] %></span><%=
              item[:explanation].present? ? content_tag(:span, ": #{item[:explanation]}", class: "text-base-content/70") : ""
            %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- NEVALIDATE Section -->
  <% if data[:nevalidate].any? %>
    <div>
      <div class="text-xs font-medium text-warning mb-2 flex items-center gap-1">
        <span class="text-sm">⚠️</span>
        <span>Informații lipsă (<%= data[:nevalidate].size %>)</span>
      </div>
      <ul class="list-disc list-inside space-y-1.5 text-xs leading-relaxed ml-1">
        <% data[:nevalidate].each do |item| %>
          <li>
            <span class="font-medium"><%= item[:criterion] %></span><%=
              item[:explanation].present? ? content_tag(:span, ": #{item[:explanation]}", class: "text-base-content/70") : ""
            %>
          </li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- ACCEPTATE Section -->
  <% if data[:acceptate].any? %>
    <div class="collapse collapse-arrow">
      <input type="checkbox" />
      <div class="collapse-title text-xs py-2 min-h-0 px-0">
        <div class="flex items-center gap-1">
          <span class="text-sm">✅</span>
          <span class="font-medium text-success">Criterii îndeplinite (<%= data[:acceptate].size %>)</span>
        </div>
      </div>
      <div class="collapse-content px-0 pt-0">
        <ul class="list-disc list-inside space-y-1.5 text-xs leading-relaxed ml-1">
          <% data[:acceptate].each do |item| %>
            <li>
              <span class="font-medium"><%= item[:criterion] %></span><%=
                item[:explanation].present? ? content_tag(:span, ": #{item[:explanation]}", class: "text-base-content/70") : ""
              %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>
</div>

<!-- Raw AI Reasoning (Collapsed) -->
<div class="collapse collapse-arrow mt-4">
  <input type="checkbox" />
  <div class="collapse-title text-xs py-2 min-h-0 px-0">
    <div class="flex items-center gap-1">
      <span class="text-sm">🔍</span>
      <span>Raționament AI complet</span>
    </div>
  </div>
  <div class="collapse-content px-0 pt-0">
    <div class="text-xs text-base-content/70 whitespace-pre-wrap leading-relaxed"><%= data[:raw_text] %></div>
  </div>
</div>
